const bulletRaw=document.getElementById('bulletRaw');const refineBulletsBtn=document.getElementById('refineBulletsBtn');const bulletOutput=document.getElementById('bulletOutput');refineBulletsBtn.addEventListener('click',()=>{const raw=bulletRaw.value.trim();if(!raw){bulletOutput.textContent='Aucun contenu à raffiner.';return}const parts=raw.split(/[.;\n]+/).map(x=>x.trim()).filter(Boolean).slice(0,6);const bullets=parts.map(p=>`- ${p.charAt(0).toUpperCase()+p.slice(1)}`);bulletOutput.textContent=bullets.join('\n')});
