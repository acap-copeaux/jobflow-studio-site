const buildInterviewPrepBtn=document.getElementById('buildInterviewPrepBtn');const interviewPrepOutput=document.getElementById('interviewPrepOutput');function loadJSON(key,fallback){try{return JSON.parse(localStorage.getItem(key)||fallback)}catch{return JSON.parse(fallback)}}buildInterviewPrepBtn.addEventListener('click',()=>{const profile=loadJSON('jobflow_profile_builder_v1','{"experiences":[],"skills":[]}');const recruiter=loadJSON('jobflow_agent_v2','{}');const lines=[];lines.push(`Poste ciblé : ${recruiter.jobTitle||'à préciser'}`);lines.push('');lines.push('Questions probables :');lines.push('- Pourquoi ce poste ?');lines.push('- Pourquoi cette entreprise ?');lines.push('- Quels éléments de votre parcours sont les plus pertinents ?');lines.push('- Comment gérez-vous les imprévus / la coordination / les priorités ?');lines.push('- Quel exemple concret illustre votre capacité à tenir ce poste ?');lines.push('');lines.push('Angles de réponse à préparer :');lines.push(`- Mettre en avant : ${(profile.skills||[]).slice(0,8).join(', ')||'compétences à préciser'}`);lines.push(`- Relier vos expériences à : ${recruiter.jobTitle||'ce poste'}`);lines.push(`- Préparer un exemple concret sur : ${(profile.experiences||[])[0]?.title||'une expérience significative'}`);lines.push('- Préparer une réponse claire sur vos écarts ou manques éventuels.');interviewPrepOutput.textContent=lines.join('\n')});
