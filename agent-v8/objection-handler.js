const objectionType=document.getElementById('objectionType');const objectionContext=document.getElementById('objectionContext');const buildObjectionBtn=document.getElementById('buildObjectionBtn');const objectionOutput=document.getElementById('objectionOutput');buildObjectionBtn.addEventListener('click',()=>{const ctx=objectionContext.value.trim()||'ce point';let text='';if(objectionType.value==='gap'){text=`Réponse suggérée : Je suis conscient que ${ctx} peut apparaître comme un point à renforcer. En revanche, mon parcours montre déjà des bases proches et transférables, et j’ai précisément préparé ma candidature pour démontrer comment je peux convertir rapidement ces acquis dans ce contexte.`}else if(objectionType.value==='pause'){text=`Réponse suggérée : Cette période ne traduit pas un désengagement mais une phase spécifique de mon parcours. Aujourd’hui, mon projet est clarifié et je reviens avec une approche plus ciblée, plus structurée et pleinement orientée vers ${ctx}.`}else if(objectionType.value==='reconversion'){text=`Réponse suggérée : Je ne présente pas cette transition comme une rupture totale, mais comme un repositionnement. Je m’appuie sur des compétences transférables concrètes déjà acquises et directement mobilisables sur ${ctx}.`}else if(objectionType.value==='experience'){text=`Réponse suggérée : Mon expérience n’est peut-être pas parfaitement linéaire sur ${ctx}, mais elle contient déjà des situations comparables. Mon enjeu est donc moins de partir de zéro que de rendre plus visible et plus lisible ce qui est déjà exploitable.`}else{text=`Réponse suggérée : Ma demande sur ${ctx} s’appuie sur le niveau de responsabilité visé, la nature du poste et la valeur que je peux apporter. Je reste ouvert à un échange si vous souhaitez préciser le cadre global de rémunération.`}objectionOutput.textContent=text});
