const buildSummaryBtn=document.getElementById('buildSummaryBtn');const summaryBuilderOutput=document.getElementById('summaryBuilderOutput');function loadJSON(key,fallback){try{return JSON.parse(localStorage.getItem(key)||fallback)}catch{return JSON.parse(fallback)}}buildSummaryBtn.addEventListener('click',()=>{const profile=loadJSON('jobflow_profile_builder_v1','{"experiences":[],"skills":[]}');const recruiter=loadJSON('jobflow_agent_v2','{}');const title=recruiter.jobTitle||'poste cible';const skills=(profile.skills||[]).slice(0,5).join(', ')||'des compétences à préciser';const exp=(profile.experiences||[])[0]?.title||'des expériences à préciser';summaryBuilderOutput.textContent=`Profil orienté ${title}, avec un socle de compétences en ${skills}. Parcours appuyé notamment sur ${exp}, avec une capacité à intervenir dans un cadre structuré, à tenir des priorités et à contribuer à des objectifs opérationnels.`});
