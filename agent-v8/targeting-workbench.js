const buildTargetingBtn=document.getElementById('buildTargetingBtn');const targetingOutput=document.getElementById('targetingOutput');function loadJSON(key,fallback){try{return JSON.parse(localStorage.getItem(key)||fallback)}catch{return JSON.parse(fallback)}}buildTargetingBtn.addEventListener('click',()=>{const recruiter=loadJSON('jobflow_agent_v2','{}');const family=document.getElementById('targetFamily').value.trim()||'famille à préciser';const status=document.getElementById('targetStatus').value.trim()||'statut à préciser';const layout=document.getElementById('targetLayout').value.trim()||localStorage.getItem('jobflow_agent_v4_layout_choice')||'mise en page à préciser';const job=recruiter.jobTitle||'poste cible';const lines=[];lines.push(`Poste cible : ${job}`);lines.push(`Famille métier : ${family}`);lines.push(`Statut / niveau : ${status}`);lines.push(`Mise en page retenue : ${layout}`);lines.push('');lines.push('Angle recommandé :');lines.push(`- Utiliser un titre ciblé aligné sur ${job}.`);lines.push(`- Mettre en avant les blocs compatibles avec la famille ${family}.`);lines.push(`- Adapter le ton et la hiérarchie au statut ${status}.`);lines.push(`- Utiliser le rendu ${layout} pour la version finale.`);targetingOutput.textContent=lines.join('\n')});
