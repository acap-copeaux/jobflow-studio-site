const buildEvidenceBtn=document.getElementById('buildEvidenceBtn');const evidenceOutput=document.getElementById('evidenceOutput');function loadJSON(key,fallback){try{return JSON.parse(localStorage.getItem(key)||fallback)}catch{return JSON.parse(fallback)}}buildEvidenceBtn.addEventListener('click',()=>{const profile=loadJSON('jobflow_profile_builder_v1','{"experiences":[],"skills":[]}');const lines=[];if(!profile.experiences.length){evidenceOutput.textContent='Aucune expérience structurée disponible.';return}profile.experiences.slice(0,10).forEach((e,i)=>{lines.push(`${i+1}. Preuve issue de : ${e.title||'Sans titre'}`);lines.push(`- Situation / cadre : ${e.date||'Période non précisée'}`);lines.push(`- Formulation courte : J’ai contribué à ${e.content||'une mission à préciser'}.`);lines.push(`- Formulation orientée valeur : Cette expérience montre ma capacité à intervenir dans un cadre opérationnel, à tenir des priorités et à produire un résultat exploitable.`);lines.push('')});evidenceOutput.textContent=lines.join('\n')});
