const buildDocumentBtn=document.getElementById('buildDocumentBtn');const copyDocumentBtn=document.getElementById('copyDocumentBtn');const downloadDocumentBtn=document.getElementById('downloadDocumentBtn');const documentPreview=document.getElementById('documentPreview');function loadJSON(key,fallback){try{return JSON.parse(localStorage.getItem(key)||fallback)}catch{return JSON.parse(fallback)}}function renderDoc(){const profile=loadJSON('jobflow_profile_builder_v1','{"experiences":[],"skills":[]}');const recruiter=loadJSON('jobflow_agent_v2','{}');const title=recruiter.jobTitle||'Titre cible';const name='Nom Prénom';const summary='Résumé à finaliser à partir du profil, du poste et des recommandations.';const skills=(profile.skills||[]).slice(0,10);const experiences=(profile.experiences||[]).slice(0,6);documentPreview.innerHTML=`<div class="doc-header"><div><div class="doc-title">${name}</div><div class="doc-subtitle">${title}</div></div><div class="doc-subtitle">${recruiter.companyName||'Entreprise cible'}</div></div><div class="doc-section"><h3>Résumé</h3><p>${summary}</p></div><div class="doc-section"><h3>Compétences</h3><p>${skills.length?skills.join(' • '):'Aucune compétence structurée'}</p></div><div class="doc-section"><h3>Expériences</h3><p>${experiences.length?experiences.map(e=>`${e.title||'Sans titre'} (${e.date||'Date non précisée'})`).join('<br>'):'Aucune expérience structurée'}</p></div><div class="doc-section"><h3>Contexte cible</h3><p>${recruiter.jobText||'Contexte cible à compléter.'}</p></div>`}async function copyDoc(){const text=documentPreview.innerText||'';if(!text)return;await navigator.clipboard.writeText(text)}function downloadDoc(){const html=`<!DOCTYPE html><html lang="fr"><head><meta charset="UTF-8"><title>Document</title></head><body>${documentPreview.innerHTML}</body></html>`;const blob=new Blob([html],{type:'text/html;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='document-built.html';a.click();URL.revokeObjectURL(url)}buildDocumentBtn.addEventListener('click',renderDoc);copyDocumentBtn.addEventListener('click',copyDoc);downloadDocumentBtn.addEventListener('click',downloadDoc);
